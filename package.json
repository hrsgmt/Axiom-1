{
  "name": "axiom",
  "private": true,
  "packageManager": "pnpm@9.1.0",
  "scripts": {
    "build:api": "pnpm --filter @axiom/api build",
    "build:web": "pnpm --filter @axiom/web build",
    "start:api": "pnpm --filter @axiom/api start",
    "start:web": "pnpm --filter @axiom/web start",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
